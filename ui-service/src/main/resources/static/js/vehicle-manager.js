document.addEventListener('DOMContentLoaded', function () {
    // Nút xem chi tiết đã được xử lý trong HTML với class 'btn-view-detail'
    // Không cần thêm event listener ở đây nữa
    
    // Export Excel - đã bị xóa nút này trong HTML
});